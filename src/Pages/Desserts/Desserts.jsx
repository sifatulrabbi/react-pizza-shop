import React from "react"
import { Products } from "../../Components"

const Desserts = ({ data }) => {
   return (
      <>
         <Products data={data} />
      </>
   )
}

export default Desserts
